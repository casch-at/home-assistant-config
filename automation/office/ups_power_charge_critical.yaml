- id: ups_battery_charge_critical
  alias: UPS battery charge critical
  description: ''
  mode: single
  trigger:
    - platform: numeric_state
      entity_id: sensor.apcups_battery_charge
      below: 10
  condition: []
  action:
    - service: hassio.host_shutdown
    # TODO(cschwarzgruber): shutdown router too?
